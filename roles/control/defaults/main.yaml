---
control:
  kubeadm_extra_args: "--pod-network-cidr=10.244.0.0/16 --cri-socket=unix:///var/run/cri-dockerd.sock --upload-certs --skip-phases=addon/kube-proxy"
  load_balancer_ip: "{{groups['kubernetes_vip_load_balancer'][0]}}"
  load_balancer_port: 6443
